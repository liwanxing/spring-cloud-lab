<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liwx.labuser.mapper.UserMapper">

    <!-- 按用户名模糊搜索 + 按状态筛选 -->
    <select id="searchByUsername" resultType="com.liwx.labuser.entity.User">
        SELECT id, username, password, email, phone, status, deleted, created_at, updated_at
        FROM users
        WHERE deleted = 0
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
