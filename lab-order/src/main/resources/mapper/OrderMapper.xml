<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liwx.laborder.mapper.OrderMapper">
    <update id="cancelOrder">
        UPDATE orders
        SET status = 'CANCELLED', updated_at = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND status = 'PENDING' AND deleted = 0
    </update>

    <update id="payOrder">
        UPDATE orders
        SET status = 'PAID', paid_at = NOW(), updated_at = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND status = 'PENDING' AND deleted = 0
    </update>

    <!-- 抢占销账（CAS 0->1）：回补是加库存、不幂等，必须先抢到回补权再动手，防并发双回补（库存虚增=可超卖） -->
    <update id="claimStockRestore">
        UPDATE orders
        SET stock_restored = 1, updated_at = NOW()
        WHERE id = #{id} AND status = 'CANCELLED' AND deleted = 0 AND stock_restored = 0
    </update>

    <!-- 抢占后回补仍失败：退账（1->0），等对账任务下一轮 -->
    <update id="releaseStockRestoreClaim">
        UPDATE orders
        SET stock_restored = 0, updated_at = NOW()
        WHERE id = #{id} AND stock_restored = 1
    </update>
</mapper>